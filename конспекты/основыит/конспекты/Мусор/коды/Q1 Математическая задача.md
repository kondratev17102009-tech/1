```Python
class Solution:
    def canMakeArithmeticProgression(self, arr: List[int]) -> bool:
        arr = sorted(arr)
        result = True
        arr_len = len(arr)
        diff = arr[0] - arr[1]
        diff2 = 0

        if arr_len == 2:
            result = True
            return result 

        for i in range(1, arr_len):

            if i + 1 != arr_len:
                diff2 = arr[i] - arr[i + 1]
                print(diff2)

            if diff != diff2:
                result = False
                return result 
                
        result = True
        return result 
```

Назначаем переменные (arr, arr_len,diff и тд.)
Делаем типо проверку 
дальше с For i in range нечего не понятно и да 