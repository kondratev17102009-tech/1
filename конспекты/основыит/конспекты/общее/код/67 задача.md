```TypeScript
function addBinary(a: string, b: string): string {

  

    let BigA = BigInt ("0b" + a)

    let BigB = BigInt ("0b" + b)

    let Sum = BigA + BigB

    let answer : string = Sum.toString(2)

    console.log(BigA, BigB, answer)

    return answer
```