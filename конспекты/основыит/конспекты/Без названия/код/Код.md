
``` Python
class Solution:

    def romanToInt(self, s: str) -> int:

        romanf = [ "I"= "t": 1 , "V"="v": 5, "X"="v": 10 , "L"="l": 50, "C"="l": 100, "D"="d": 500, "M"="d": 1000]

        num = 0

        prev_roman = " "  

    for c in s:

        prev_roman == c

        if prev_roman == "I" and c in ("V", "X"):

            num += ramanf.get(c) - 2

            print(num, "I")print(num, "I")

         if prev_roman == "X"  and c in ("L", "C"):

            num += ramanf.get(c) - 20

            print(num, "X")

         if prev_roman == "C"  and c in ("D", "M"):

            num += ramanf.get(c) - 200

            print(num, "C")

        else:

            num += romanf.get(c)

            print(num,"ffs",prev_roman,c)

    return num


```



``` Python
class Solution:

    def longestCommonPrefix(self, strs: List[str]) -> str:

     counter = 0

     prefix = ""

     strs = sorted(strs)

     print(strs)

def prefix_in_list(self,word: str,strs: List[str]):

  

    for i in range(len(word))


```

class Node:

     def __init__(self, val=0, next=None):

         self.val = val

         self.next = next

  

class linkedList:

    def __init__(self):

        self.head = None

    def append(self, value):

        new_node = Node(value)

  

        if not self.head:

            self.head = new_node

              return

  

    current = self.head

    while current.next:

        current = current.next

    current.next = new_node

  

    def __str__(self):

        nodes_str = ''

        current_node = self.next

    current.next = new_node    

  

class Solution:

    def mergeTwoLists(self, list1: Optional[ListNode], list2: Optional[ListNode]) -> Optional[ListNode]:





is_running = True






#while is_running:

#    raw = input(" jrfr")

#    if raw == "exit":

#        is_running = False

#        print("fdsfs")

#    elif raw == "Продолжить":        

#     x = int(input())

#     y = int(input())

#     z = 0

#    

#    try:

#        z = x/y

#    except ZeroDivisionError:

#            print("Исключение")

#   else:

#        print("Некоректный код")

import random

  

class adntobot:

  

    def __init__(self):

        self.player = (("play1" , "x"), ("play2" , "y"))

        self.current = random.choice(self.player)

        self.fielld = [

        [" "," ",[" ",]  ]

        [" "," ",[" ",]  ]

        [" "," ",[" ",]  ]

    ]

def print_frield(self):

    out = ''

    for in range(10):

        for j in range(4):

            out += self.field[i], [j] + " "

        print(out)

        out = " "

  

class androbot:

    def __init__(self):

        self.deley = ()


===

is_running = True

  

#while is_running:

#    raw = input(" jrfr")

#    if raw == "exit":

#        is_running = False

#        print("fdsfs")

#    elif raw == "Продолжить":        

#     x = int(input())

#     y = int(input())

#     z = 0

#    

#    try:

#        z = x/y

#    except ZeroDivisionError:

#            print("Исключение")

#   else:

#        print("Некоректный код")

  

import random

  

class adntobot:

  

    def __init__(self):

        self.player = (("play1" , "x"), ("play2" , "y"))

        self.current = random.choice(self.player)

        self.fielld = [

        [" "," ",[" ",]  ]

        [" "," ",[" ",]  ]

        [" "," ",[" ",]  ]

    ]

def print_frield(self):

    out = ''

    for in range(10):

        for j in range(4):

            out += self.field[i], [j] + " "

        print(out)

        out = " "

  

def select_current(self):

    if self.current == 1:

        self.current == 0

    else:

        self.current == 1

    pass

def input_coords(self , player , x , y):

    if x and y < 3:

        if self.field[x][y] == "_":

            self.field[x][y] == self.player[player]

            return 1

        else:

            print("sadada")

            return 0

    else:

        print("sdsada")

        return 0





import random , time , os  

  

class Hangmen:

  

    def __init__(self , words_par):

        self.words = words_par

        self.attemps = 12

        self.selected_words = random.choice(self.words)

        self.guuessed_string =  "_" * len(self.selected_words)

  

    def main(self):

  

        is_ranning = True

        while is_ranning == True:

            print("Добро пожаловать")

            print("fsdfs")

            user_input = input("fdfsd")

            if user_input == "fgdgd" :

               is_running = False

  

            while self.attemps != 0:

                os.system("cls")

                print("Осталосьпопвток" , self.attemps )

                print(self.guuessed_string)

                letter = input("Введите букву")

  

                if self.guuessed_string.count(letter):

                    for i in range(len(self.selected_words)):

                        self.guuessed_string = ""

  

                if self.selected_words.count(letter):

                   for i in range(len(self.selected_words)):

                        if self.selected_words[i] == letter:

                           self.guuessed_string += letter

                        elif self.selected_words[i] != "_" :

                           pass

                        else:

                           self.guuessed_string += "_"

                else:

                    self.attemps -= 1

                    pass              

  
  
  
  

    def testing(self):

        os.system("cls")

        print (self.words)

        print (self.attemps)

        print (self.selected_words)

        print (self.guuessed_string)

words_list = ["ffffffff", "gggggggggggggggg", "hhhhhhh"]

s = Hangmen(words_list)

s.testing()



```Python
import pandas as pd
import numpy as np

d = {"a": 10, "b": 20, "c": 30, "g": 40}
print(pd.Series(d))

print(pd.Series(d, index=["a", "b", "c", "d"]))

index = ["a", "b", "c"]

print(pd.Series("окак", index = index))


s = pd.Series(np.arange(9), index=["a", "b", "c", "d", "e","s","h","j","u"])
#print(s["a"])             
#print(s[["a", "d"]])      
#print(s[1:])              
print(s + s) 
```