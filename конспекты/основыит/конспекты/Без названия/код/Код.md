
``` Python
class Solution:

    def romanToInt(self, s: str) -> int:

        romanf = [ "I"= "t": 1 , "V"="v": 5, "X"="v": 10 , "L"="l": 50, "C"="l": 100, "D"="d": 500, "M"="d": 1000]

        num = 0

        prev_roman = " "  

    for c in s:

        prev_roman == c

        if prev_roman == "I" and c in ("V", "X"):

            num += ramanf.get(c) - 2

            print(num, "I")print(num, "I")

         if prev_roman == "X"  and c in ("L", "C"):

            num += ramanf.get(c) - 20

            print(num, "X")

         if prev_roman == "C"  and c in ("D", "M"):

            num += ramanf.get(c) - 200

            print(num, "C")

        else:

            num += romanf.get(c)

            print(num,"ffs",prev_roman,c)

    return num


```



``` Python
class Solution:

    def longestCommonPrefix(self, strs: List[str]) -> str:

     counter = 0

     prefix = ""

     strs = sorted(strs)

     print(strs)

def prefix_in_list(self,word: str,strs: List[str]):

  

    for i in range(len(word))


```

class Node:

     def __init__(self, val=0, next=None):

         self.val = val

         self.next = next

  

class linkedList:

    def __init__(self):

        self.head = None

    def append(self, value):

        new_node = Node(value)

  

        if not self.head:

            self.head = new_node

              return

  

    current = self.head

    while current.next:

        current = current.next

    current.next = new_node

  

    def __str__(self):

        nodes_str = ''

        current_node = self.next

    current.next = new_node    

  

class Solution:

    def mergeTwoLists(self, list1: Optional[ListNode], list2: Optional[ListNode]) -> Optional[ListNode]:





is_running = True






#while is_running:

#    raw = input(" jrfr")

#    if raw == "exit":

#        is_running = False

#        print("fdsfs")

#    elif raw == "Продолжить":        

#     x = int(input())

#     y = int(input())

#     z = 0

#    

#    try:

#        z = x/y

#    except ZeroDivisionError:

#            print("Исключение")

#   else:

#        print("Некоректный код")

import random

  

class adntobot:

  

    def __init__(self):

        self.player = (("play1" , "x"), ("play2" , "y"))

        self.current = random.choice(self.player)

        self.fielld = [

        [" "," ",[" ",]  ]

        [" "," ",[" ",]  ]

        [" "," ",[" ",]  ]

    ]

def print_frield(self):

    out = ''

    for in range(10):

        for j in range(4):

            out += self.field[i], [j] + " "

        print(out)

        out = " "

  

class androbot:

    def __init__(self):

        self.deley = ()